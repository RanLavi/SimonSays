import { Dimensions } from 'react-native';

export const { height: deviceHeight, width: deviceWidth } = Dimensions.get('window');
export const { height: deviceScreenHeight } = Dimensions.get('screen');